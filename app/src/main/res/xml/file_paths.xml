<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External storage paths -->
    <external-path
        name="external"
        path="." />
    
    <!-- Cache directories -->
    <cache-path
        name="cache"
        path="." />
    
    <!-- External files paths -->
    <external-files-path
        name="external_files"
        path="." />
        
    <!-- External pictures directory -->
    <external-files-path
        name="pictures"
        path="Pictures" />
    
    <!-- External documents directory -->
    <external-files-path
        name="documents"
        path="Documents" />
    
    <!-- App-specific directories -->
    <external-files-path
        name="smartdocs_images"
        path="Pictures/SmartDocsConvert" />
    
    <!-- External cache directory -->
    <external-cache-path
        name="external_cache"
        path="." />
    
    <!-- Files path -->
    <files-path
        name="files"
        path="." />
</paths> 